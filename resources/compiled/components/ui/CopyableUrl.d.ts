/// <reference types="react" />
declare type Props = {
    url: string;
    helpText: string;
    openText: string;
};
export default function CopyableUrl({ url, openText, helpText }: Props): JSX.Element;
export {};
