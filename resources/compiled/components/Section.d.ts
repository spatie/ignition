import React from 'react';
declare type Props = {
    children: React.ReactNode;
    className?: string;
    name: string;
};
export default function Section({ children, className, name }: Props): JSX.Element;
export {};
